class Player{
  constructor(name, token){
    this.name = name;
    this.token = token;
    this.score = 0;
    this.figher = "undecided";
  }

  choseFighter(fighter){
    //when player chooses a fighter
  }

  updateScore(){
    this.score++;
  }
}
